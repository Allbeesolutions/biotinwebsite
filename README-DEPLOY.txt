BIOTIN CLINIC SEO PACKAGE

1. Deploy all files in this folder to the website root.
2. index.html replaces the old homepage HTML.
3. Keep seo-pages.css beside the HTML files.
4. robots.txt and sitemap.xml must be at the domain root.
5. In Search Console, submit https://www.biotinclinic.com/sitemap.xml
6. After deployment, inspect the homepage and important new pages, then request indexing once for changed/new pages.

Important: Doctor details are copied from the supplied clinic HTML. Verify current names, qualifications and timings before publishing any changes.
